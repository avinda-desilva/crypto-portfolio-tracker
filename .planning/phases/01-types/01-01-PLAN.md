---
phase: 01-types
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - types/wallet.ts
autonomous: true
requirements:
  - TYPES-01

must_haves:
  truths:
    - "Wallet interface is exported from types/wallet.ts with exactly two fields: address (string) and chain (Chain union)"
    - "PortfolioResult interface is exported from types/wallet.ts with exactly three fields: address (string), chain (string), balance (number)"
    - "Chain type alias is exported and defined as the union 'ethereum' | 'bitcoin' | 'solana'"
    - "No usdValue, id, or error state fields exist on either interface"
    - "TypeScript compiles the file with no errors"
  artifacts:
    - path: "types/wallet.ts"
      provides: "Wallet and PortfolioResult interfaces plus Chain type alias"
      exports:
        - "Chain"
        - "Wallet"
        - "PortfolioResult"
  key_links:
    - from: "types/wallet.ts"
      to: "app/page.tsx (Phase 2)"
      via: "import { Wallet } from '@/types/wallet'"
      pattern: "export interface Wallet"
    - from: "types/wallet.ts"
      to: "app/api/portfolio/route.ts (Phase 4)"
      via: "import { PortfolioResult } from '@/types/wallet'"
      pattern: "export interface PortfolioResult"
    - from: "types/wallet.ts"
      to: "lib/ethereum.ts, lib/bitcoin.ts, lib/solana.ts (Phases 5-7)"
      via: "import { PortfolioResult } from '@/types/wallet'"
      pattern: "export interface PortfolioResult"
---

<objective>
Create the single shared TypeScript contracts file that all subsequent phases import.

Purpose: Lock the data shapes for `Wallet` and `PortfolioResult` now so Phases 2-14 build against a stable, agreed-upon interface. This file contains zero runtime logic — it is pure type definitions.

Output: `types/wallet.ts` exporting `Chain`, `Wallet`, and `PortfolioResult`.
</objective>

<execution_context>
@/Users/avinda/Documents/github/crypto-portfolio-tracker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/avinda/Documents/github/crypto-portfolio-tracker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-types/01-CONTEXT.md

**Decision summary (authoritative — these override ROADMAP.md's older field list):**
- D-01: Two interfaces only: `Wallet` and `PortfolioResult`
- D-02: `Wallet` fields: `address: string`, `chain: "ethereum" | "bitcoin" | "solana"`
- D-03: `PortfolioResult` fields: `address: string`, `chain: string`, `balance: number`
- D-04: Chain literals are lowercase full names — NOT "ETH"/"BTC"/"SOL"
- D-05: No `id` field on `Wallet`
- D-06: No `usdValue` field
- D-07: No error state fields
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create types/wallet.ts with Chain, Wallet, and PortfolioResult</name>
  <files>types/wallet.ts</files>

  <read_first>
    - .planning/phases/01-types/01-CONTEXT.md — authoritative field specs (D-01 through D-07)
    - .planning/ROADMAP.md — Phase 1 success criteria and downstream import sites
  </read_first>

  <behavior>
    - Chain is exactly: type Chain = "ethereum" | "bitcoin" | "solana"
    - Wallet has exactly two fields: address (string), chain (Chain) — no id, no usdValue, no error
    - PortfolioResult has exactly three fields: address (string), chain (string), balance (number) — no usdValue, no error
    - All three names are exported (named exports, not default exports)
    - File compiles with tsc --noEmit without errors
    - No fields exist beyond the exact specs above
  </behavior>

  <action>
Create the directory `types/` at the repo root and write `types/wallet.ts` with the following exact content (verbatim — no additions):

```typescript
/**
 * Shared TypeScript contracts for the crypto portfolio tracker.
 * All phases import from this file. Zero runtime logic — pure types only.
 */

/** Supported blockchain networks. Values are lowercase full names. */
export type Chain = "ethereum" | "bitcoin" | "solana";

/**
 * A user-defined wallet entry.
 * Identity is the composite key: address + chain.
 * No id field — address+chain uniquely identifies each wallet.
 */
export interface Wallet {
  address: string;
  chain: Chain;
}

/**
 * The result of a balance lookup for one wallet.
 * Returned by chain modules (lib/ethereum.ts, lib/bitcoin.ts, lib/solana.ts)
 * and aggregated by app/api/portfolio/route.ts.
 */
export interface PortfolioResult {
  address: string;
  chain: string;
  balance: number;
}
```

Key implementation notes:
- `Wallet.chain` uses the `Chain` type alias (not the raw union literal repeated inline) — this satisfies D-02 and D-04 while avoiding literal duplication
- `PortfolioResult.chain` is `string` (not `Chain`) — downstream chain modules may pass this value without needing to import Chain
- Do NOT add: `id`, `usdValue`, `timestamp`, `error`, `status`, or any field not in the spec above (D-05, D-06, D-07)
- Do NOT use default export — all three are named exports
  </action>

  <verify>
    <automated>cd /Users/avinda/Documents/github/crypto-portfolio-tracker && npx tsc --noEmit --strict --target ES2020 --module NodeNext --moduleResolution NodeNext types/wallet.ts 2>&1 || echo "TSC_RESULT: $?"</automated>
  </verify>

  <acceptance_criteria>
    - `types/wallet.ts` exists at repo root level (not inside `src/` or any subdirectory)
    - File contains `export type Chain = "ethereum" | "bitcoin" | "solana"`
    - File contains `export interface Wallet {` with exactly: `address: string;` and `chain: Chain;` — verified by grep
    - File contains `export interface PortfolioResult {` with exactly: `address: string;`, `chain: string;`, and `balance: number;` — verified by grep
    - `grep -c "id:" types/wallet.ts` returns 0 (no id field)
    - `grep -c "usdValue" types/wallet.ts` returns 0 (no usdValue field)
    - `grep -c "error" types/wallet.ts` returns 0 (no error field, ignoring JSDoc)
    - `grep -c "export" types/wallet.ts` returns 3 (exactly three named exports: Chain, Wallet, PortfolioResult)
    - `grep -c "default" types/wallet.ts` returns 0 (no default exports)
    - TypeScript compiler accepts the file: `npx tsc --noEmit --strict types/wallet.ts` exits 0 or produces no type errors
  </acceptance_criteria>

  <done>
    types/wallet.ts exists with exactly three exports (Chain, Wallet, PortfolioResult), all fields match the D-01 through D-07 spec exactly, and tsc reports no type errors on the file.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| compile-time only | This file contains zero runtime logic — no trust boundaries at runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | types/wallet.ts | accept | Pure type file; no runtime attack surface. Types are erased at compile time. Field constraints enforced by TypeScript at build time across all consuming modules. |
</threat_model>

<verification>
After the task completes, run these checks in sequence:

1. File exists:
   `ls types/wallet.ts` — must print the path

2. Three exports and no extras:
   `grep "^export" types/wallet.ts` — must print exactly three lines (Chain, Wallet, PortfolioResult)

3. No forbidden fields:
   `grep -v '^[[:space:]]*//' types/wallet.ts | grep -c "id:"` — must return 0
   `grep -c "usdValue" types/wallet.ts` — must return 0
   `grep -c "error:" types/wallet.ts` — must return 0

4. Chain literals are correct (lowercase full names, not abbreviations):
   `grep "Chain" types/wallet.ts | grep -c '"ethereum"'` — must return 1
   `grep "Chain" types/wallet.ts | grep -c '"ETH"'` — must return 0

5. TypeScript compilation:
   `npx tsc --noEmit --strict types/wallet.ts` — must exit 0 with no output
</verification>

<success_criteria>
Phase 1 is complete when:
1. `types/wallet.ts` exports `Chain`, `Wallet`, and `PortfolioResult` — confirmed by grep
2. `Wallet` has exactly `address: string` and `chain: Chain` — no extra fields
3. `PortfolioResult` has exactly `address: string`, `chain: string`, `balance: number` — no extra fields
4. Chain values in the type are `"ethereum"`, `"bitcoin"`, `"solana"` (per D-04) — not abbreviations
5. TypeScript compiles the file with strict mode and zero errors
6. No id, usdValue, or error fields exist on any interface (per D-05, D-06, D-07)
</success_criteria>

<output>
After completion, create `.planning/phases/01-types/01-01-SUMMARY.md` using the summary template.
</output>
